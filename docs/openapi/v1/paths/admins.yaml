AdminsWorksImagesById:
  patch:
    operationId: patchWorksImagesById
    tags:
      - admins
    summary: IDを指定して作品の画像のURLを追加する
    description: |
      IDを指定して作品の画像を追加する。
    requestBody:
      description: |-
        ### User
      required: true
      content:
        application/json:
          schema:
            $ref: "../components/schemas/works.yaml#/ApiWorkImage"
    responses:
      "200":
        $ref: "../components/responses/works.yaml#/WorksIdResponse"
      "400":
        $ref: "../components/responses/errors.yaml#/BadRequestResponse"
      "401":
        $ref: "../components/responses/errors.yaml#/UnauthorizedResponse"