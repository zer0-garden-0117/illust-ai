UsersResponse:
  description: |-
    200 OK
    - 正常終了
  content:
    application/json:
      schema:
        $ref: "../schemas/users.yaml#/ApiUser"

UsersWorksResponse:
  description: |-
    200 OK
    - 正常終了
  content:
    application/json:
      schema:
        $ref: "../schemas/works.yaml#/ApiWorks"

FollowUsersResponse:
  description: |-
    200 OK
    - 正常終了
  content:
    application/json:
      schema:
        $ref: "../schemas/users.yaml#/ApiFollowUsers"

UsersLikedByWorkIdResponse:
  description: |-
    200 OK
    - 正常終了
  content:
    application/json:
      schema:
        $ref: "../schemas/users.yaml#/ApiLiked"

UsersTagResponse:
  description: |-
    200 OK
    - 正常終了
  content:
    application/json:
      schema:
        $ref: "../schemas/works.yaml#/ApiTag"

UsersTagsResponse:
  description: |-
    200 OK
    - 正常終了
  content:
    application/json:
      schema:
        $ref: "../schemas/users.yaml#/ApiTaggeds"

UsersIsTagResponse:
  description: |-
    200 OK
    - 正常終了
  content:
    application/json:
      schema:
        $ref: "../schemas/works.yaml#/ApiIsTag"
